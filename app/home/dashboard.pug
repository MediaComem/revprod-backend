p.lead.text-success Welcome to the Corsican Charity!

.row.mt-4

  //- Donation form
  .col-12.col-lg-6
    h3 Donate
    form(action='/donations', method='POST')
      //- Amount field
      .mb-3
        label.form-label(for='donation-amount') Amount
        input#donation-amount.form-control(
          type='number',
          name='amount',
          required,
          min=0
        )

      //- Actions
      button.btn.btn-primary(type='submit') Donate

  .row.mt-4

    //- Donations
    .col-12
      h3 Donations
      table.table.table-striped.table-hover
        thead
          tr
            th Date
            th Amount
        tbody
          if donations.length === 0
            tr
              td(colspan=4) No donations
          each donation in donations
            tr
              td= donation.date
              td= donation.amount
