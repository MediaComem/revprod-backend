{
  "type": "object",
  "properties": {
    "amount": {
      "type": "integer",
      "minimum": 1,
      "errorMessage": {
        "minimum": "Domation amount must be greater than or equal to 1"
      }
    }
  },
  "required": ["amount"],
  "additionalProperties": false
}
