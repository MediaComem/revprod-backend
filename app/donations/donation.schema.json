{
  "type": "object",
  "properties": {
    "amount": {
      "type": "integer",
      "minimum": 1,
      "errorMessage": {
        "minimum": "Domation amount must be greater than or equal to 1"
      }
    },
    "testimonial": {
      "type": "string",
      "minLength": 1,
      "maxLength": 5000,
      "errorMessage": {
        "minLength": "Testimonial must have at least 1 character",
        "maxLength": "Testimonial must not have more than 5,000 characters"
      }
    }
  },
  "required": ["amount"],
  "additionalProperties": false
}
